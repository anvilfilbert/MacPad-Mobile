# Domain Docs

PhonePad uses a single-context layout.

Before work:

- Read `CONTEXT.md`.
- Read relevant ADRs under `docs/adr/`.
- Use glossary vocabulary in code, tests, specifications, and tickets.
- Surface ADR conflicts explicitly instead of silently overriding them.
