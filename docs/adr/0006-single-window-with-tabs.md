# Single window with tabs

PhonePad uses one app window containing all open Tabs on both iPhone and iPad. MacPad's separate window groups are treated as macOS-specific rather than reproduced on iPad; this keeps navigation and Document Recovery consistent across devices and avoids scene-restoration behavior conflicting with PhonePad's explicit rejection of Session Restore.
